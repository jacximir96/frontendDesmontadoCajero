import { FilterItemsFormaPagoPipe } from './filter-items-forma-pago.pipe';

describe('FilterItemsFormaPagoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemsFormaPagoPipe();
    expect(pipe).toBeTruthy();
  });
});
